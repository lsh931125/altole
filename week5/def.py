# 함수 (def(definition), function)

# def 함수명(매개변수1, 매개변수2,...)
#   실행될 함수 소스

# 매개변수는 있을수도 있고 없을수도 있다.
# 함수를 사용하는 목적 - 재사용

# 함수를 정의
# 함수를 정의한다고 해서 함수가 실행되지 않는다.
def add(a,b):
    print(a+b)

# 함수를 호출
# 함수명(매개변수1,매개변수2,...)
add(3,5)
add(10,20)
